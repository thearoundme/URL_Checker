apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-check-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: url-check-frontend
  template:
    metadata:
      labels:
        app: url-check-frontend
    spec:
      containers:
        - name: frontend
          image: url-check-frontend:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: url-check-frontend
spec:
  selector:
    app: url-check-frontend
  ports:
    - port: 80
      targetPort: 80
